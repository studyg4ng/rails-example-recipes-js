<h1>Zutaten-Liste</h1>

<ul>
  <% @ingredients.each do |ingredient| %>
    <li><%= link_to 'Edit', edit_ingredient_path(ingredient) %>
    | <%= link_to 'Destroy', ingredient, method: :delete, data: { confirm: 'Are you sure?' } %>
    | <%= ingredient.name %>
    </li>
  <% end %>
</ul>


<%= link_to 'New Ingredient', new_ingredient_path %>
